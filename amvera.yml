meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  commands:
    - npm install
    - npm run build

run:
  command: node src/server.js
  port: 5001
  persistenceMount: /data
