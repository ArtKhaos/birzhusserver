meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  commands:
    - npm ci
  artifacts:
    "*": "/"

run:
  command: node src/server.js
  port: 5001
  persistenceMount: /data
